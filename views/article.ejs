<%- include header.ejs %>

<p class="text-info"><h2><a href="/article/<%= post._id %> "><%= post.title %></a></h2>
<%if(user&&user.name===post.name){%>
<a href="/edit/<%= post._id %>"> 编辑</a> <br>
<a href="/remove/<%= post._id %>"> 删除</a><br>
<%}%>

作者：<a href="/u/<%= post.name %>"><%= post.name %></a>

<p>
    日期：
    <small><%= post.time %></small>
<p>
    标签：
    <% post.tags.forEach(function (tag, index) { %>
    <% if(tag){ %>
    <% } %>
    <a class="tag" href="/tags/<%= tag %>"><%= tag %></a>
    <% }) %>

</p>
</p>
<p><%- post.post %></p>
<p class="info">阅读：<%=post.pv%>|评论：<%=post.comments.lenght%></p>
<%- include comment.ejs%>
<%- include footer.ejs %>